# PypiloteRemote
This app allow to control pypilote with android, linux, osx or windows.
It find your pypilote in your local network with zeroconf.
The code use the framework Briefcase with toga.
It allow you to compile the same source file in python to all the OS.
in the the build forlder you can find app for macos and android

